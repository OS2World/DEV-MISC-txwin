This is a minimal cross-platform sample program like 'Hello World'
to be built using OpenWatcom for OS/2, Windows, DOS(32) and Linux
or with either GCC or LLVM/Clang for macOS

Build using'build.cmd', or through 'b.cmd' like 'b all'
or on macOS, 'make' or 'make all'
